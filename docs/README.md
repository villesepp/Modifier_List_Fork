This is a vibe-fixed fork of the handy Modifier List add-on.

The fix restores the functionality to add a modifier using the search that is visible immediately (as opposed to the one that is behind a click of the "+" button).

Briefly tested using Blender 5.1.2, and seems to work.
